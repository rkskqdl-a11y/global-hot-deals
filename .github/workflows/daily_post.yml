name: Daily Ali Post Bot (4x Daily)

on:
  schedule:
    # ⏰ 한국 시간(KST) 기준: 09시, 15시, 21시, 03시 실행 (UTC 0, 6, 12, 18)
    - cron: '0 0,6,12,18 * * *'
  workflow_dispatch: # 🚀 언제든 수동으로 실행할 수 있는 버튼 활성화

permissions:
  contents: write # 📝 새 포스트와 사이트맵 파일을 저장하기 위해 필수

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4 # 안정적인 최신 v4 버전 사용

      - name: Set up Python
        uses: actions/setup-python@v5 # 안정적인 최신 v5 버전 사용
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run Ali Bot
        env:
          # 🔑 GitHub Secrets에 등록된 이름과 파이썬 변수명을 일치시켰습니다.
          ALI_APP_KEY: ${{ secrets.ALI_APP_KEY }}
          ALI_SECRET: ${{ secrets.ALI_SECRET }}
          ALI_TRACKING_ID: ${{ secrets.ALI_TRACKING_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python main.py

      - name: Commit and Push Changes
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          
          # ✅ 오타 수정: 'git add.' -> 'git add .' (공백 필수)
          git add .
          
          # ✅ 오타 수정: 'if!' -> 'if !' (공백 필수)
          # 변경 사항(새 글, 사이트맵)이 있는 경우에만 커밋합니다.
          if ! git diff-index --quiet HEAD; then
            git commit -m "chore: Daily Auto-Update (Posts & SEO Files) $(date +'%Y-%m-%d')"
            git push
          else
            echo "No changes detected. Skipping push."
          fi
